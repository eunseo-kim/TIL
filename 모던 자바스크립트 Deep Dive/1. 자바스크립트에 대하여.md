# 📝자바스크립트에 대하여(1-3장) / TIL(21.02.26)

## 📌 자바스크립트의 특징
- 자바스크립트는 **명령형, 함수형, 프로토타입 기반 객체지향 프로그래밍**을 지원하는 멀티 패러다음 프로그래밍 언어이다.
- 자바스크립트는 개발자가 별도의 컴파일 작업을 수행하지 않는 **인터프리터 언어**이다.
- 자바스크립트는 인터프리터 언어이지만, 일부 소스코드는 컴파일하여 실행한다.
- 자바스크립트는 인터프리터 언어의 장점인 '동적 기능 지원'을 살리면서, 실행속도가 느리다는 단점은 극복한다.
- 자바스크립트는 런타임에 컴파일 되며 실행파일이 생성되지 않고 인터프리터의 도움 없이 실행할 수 없다.

### 🔍 인터프리터 언어 vs. 컴파일러 언어
> 💡 **인터프리터 언어의 특징**
> - 코드가 실행되는 단계인 런타임에 한 줄씩 중간 코드인 바이트코드로 변환한 후 실행된다.
> - 실행 파일을 생성하지 않는다.
> - 인터프리트 단계와 실행 단계가 분리 되어 있지 않다. 한줄씩 변환하고 즉시 실행한다.
> - 코드가 실행될 때마다 인터프리트 과정이 반복 수행된다.
> - 인터프리트 단계와 실행 단계가 분리되어 있지 않고 반복 수행되어 코드 실행 속도가 비교적 느리다.

> 💡 **컴파일러 언어의 특징**
> - 코드가 실행되기 전 단계인 '컴파일 타임'에 소스 코드 전체를 하번에 커신 코드로 변환한 후 실행한다.
> - 실행 파일을 생성한다.
> - 컴파일 단계와 실행 단계가 분리되어 있다. 명시적인 컴파일 단계를 거치고 명시적으로 실행 파일을 실행한다.
> - 실행에 앞서 컴파일은 '한 번'만 수행된다.
> - 컴파일 단계와 실행 단계가 분리되어 있으므로 코드 실행 속도가 빠르다.


## 📌 자바스크립트와 ECMAScript
- 자바스크립트는 일반적으로 프로그래밍 언어로서 기본 뼈대를 이루는 ECMAScript와 브라우저가 별도로 지원하는 클라이언트 사이트 Web API를 아우르는 개념이다.

## 📌 브라우저와 Node.js
- 브라우저: html, css, js를 실행해 웹 페이지를 브라우저 화면에 렌더링하는 것이 주 목적이다.
- 브라우저는 ECMAScript와 클라이언트 사이드 Web API(DOM, BOM, Canvas 등)를 지원한다.
- Node.js: 브라우저 '외부'에서 자바 스크립트 실행 환경을 제공하는 것이 주 목적이다.
- Node.js는 ECMAScript와 Node.js 고유의 API를 지원한다.
> **렌더링**? html, css, js로 작성된 문서를 해석해서 브라우저에 시각적으로 출력하는 것
